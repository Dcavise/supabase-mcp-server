services:
  - type: web
    name: primer-internal-mcp-server
    env: docker
    dockerfilePath: Dockerfile
    startCommand: supabase-mcp-server --host 0.0.0.0 --port ${PORT}
    ports:
      - ${PORT}

